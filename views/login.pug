<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title My bookshop
    link(rel="stylesheet" href="/css/styles.css")
    script(src="https://code.jquery.com/jquery-3.4.1.slim.min.js")
    script(src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js")
    script(src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js")
    script(src='https://kit.fontawesome.com/fe90d62b6d.js')
  body.landing-body
    nav.navbar.navbar-expand-lg.navbar-dark.bg-opacity-dark.py-4
      .container
        a.navbar-brand.font-weight-bold(href='/') My Bookshop
    .container.py-md-5.py-2
      .d-flex.flex-row
        .landing-card.m-md-3.m-2.d-flex.flex-fill.justify-content-center.align-items-center.p-md-4.p-3.flex-column.text-center
          h1.font-weight-bold Login
          h4 Signin to buy books in store.
          form.needs-validation.w-75.my-4(novalidate action="/auth/login" method="post")
            .mb-2
              input.form-control(type='email' placeholder="E-Mail" name="email" required)
              .invalid-feedback Please include a valid email.
            .mb-2
              input.form-control(type='password' placeholder="Password" name="password" minlength="6" required)
              .invalid-feedback Please enter a password with 6 or more characters.
            .form-group.form-check
              input.form-check-input#checkOut(type='checkbox')
              label.form-check-label(for='checkOut') Check me out
            button.btn.btn-primary.w-100(type='submit') Submit
          h5
            | If you don't have account.
            br
            | please 
            a.font-weight-bold(href='/auth/register') Signup
            |  here.
    script.
      (function() {
        'use strict';
        window.addEventListener('load', function() {
          var forms = document.getElementsByClassName('needs-validation');
          var validation = Array.prototype.filter.call(forms, function(form) {
            form.addEventListener('submit', function(event) {
              if (form.checkValidity() === false) {
                event.preventDefault();
                event.stopPropagation();
              }
              form.classList.add('was-validated');
            }, false);
          });
        }, false);
      })();